# Soundgram Playlist Parser


Микросервис для парсинга плейлистов YandexMusic для интеграции с интерфейсом SoundGram.

# 🚀 Быстрый старт

Требования:

- Python 3.8
- requirements.txt

Для запуска приложения в режиме разработки:
> **::Terminal::** *fastapi dev app.py*

Сервис будет доступен локально на http://127.0.0.1:8000

# 🗒️ Описание микросервиса

API предоставляет возможность получить нормализованный JSON с информацией о плейлисте и его треках по ссылке на сам плейлист.

**Важно**: все ответы от сервера приходят в формате JSON

Основной эндпоинт для запроса JSON-информации о плейлисте:

> **GET /get_playlist_info/{playlist_link}**

Варианты ответов:
- 200: успешное получение информации о плейлисте и его нормализация
- 400: Ошибка со стороны пользователя. Происходит в двух случаях: Неверный формат ссылки; Пустой плейлист

- 404: Плейлист не найден
- 500: Ошибка на сервере

Структура ответа:

- Коды 400/404/500: JSON с ключом "detail", по которому доступны ключи case (Краткий тип ошибки) и message (более подробное объяснение ошибки)
- Код 200:
<img width="1173" height="578" alt="Untitled" src="https://github.com/user-attachments/assets/3d2e0b1b-9c5e-4882-8773-38960ac8031d" />


Поддерживаемые форматы ссылок:

- Старый формат: https://music.yandex.ru/users/{owner}/playlists/{kinds}
- Новый формат: https://music.yandex.ru/playlists/{lk.id}

**Важно:** ссылки с query-параметрами также корректно обрабатываются (*например ссылка из приложения при копировании по кнопке имеет формат* https://music.yandex.ru/playlists/{lk.id}?utm_source=web&utm_medium=copy_link)

Поступившая ссылка проверяется на соответствие одному из форматов посредством регулярных выражений, затем происходит запрос к API яндекс музыки:


- Для ссылок старого формата: https://music.yandex.ru/handlers/playlist.jsx?owner={owner}&kinds={kinds}
- Для ссылок старого формата: https://api.music.yandex.by/playlist/{lk.id}?resumestream=false&richtracks=true

Запросы отправляются с помощью модуля httpx. Опытным путем выяснилось, что для получения данных по ссылке старого формата в заголовках запроса нужно передать кук с айди сессии:
*headers = {
        ...,
        'cookie': 'Session_id=...'}*
        
Информация об этом куке изучена не до конца, скорее всего он может достичь срока годности, при этом возможно он может жить порядка месяца. Но известно, что в нем хранится информация об аккаунте, поэтому он должен храниться вне кода. Так как возможно его придется менять время от времени, хранить в среде окружения будет не лучшим решением, поэтому он хранится в файле конфига: config/config.json

В ходе тестов было замечено, что иногда API яндекс музыки может вернуть страницу с чем-то в роде капчи, при этом достаточно рандомно. Поэтому каждый запрос делается с 3 повторными попытками (retry). При неудачной попытке следующая повторяется через 10 секунд. 

**Важно**: количество попыток и время повторения можно изменить в config.json

При запросах обрабатываются некоторые варианты ошибок (таймаут, ненайденный плейлист, неопознанные ошибки), каждая из которых обрабатывается сервисом.

Также был добавлен логгер из базового модуля logging для дебага и отслеживания возможных ошибок. В стоке он настроен на DEBUG-уровень, но он достаточно шумный, поэтому для продакшена уровень можно повысить до INFO

# ⚡ Assumptions & Edge cases

**Какие ссылки и форматы поддержаны**:
- Ссылки старого формата
- Ссылки старого формата с query-параметрами
- Ссылки нового формата
- Ссылки нового формата с query-параметрами

**Почему**:
Пользователь может отправить скопированную ссылку, в которой будут query параметры, которые никак не влияют на получение плейлиста. Поэтому вместо повторного запроса ссылки эти параметры отбрасываются

**Какие краевые случаи учтены**:
- Принимаются ссылки с query-параметрами и без них
- Отдельный ответ для неверной ссылки
- Отдельный ответ для пустого плейлиста
- Отдельный ответ для несуществующего плейлиста
- Если структура ответа от API яндекс музыки поменяется, это можно будет быстро отследить по логам
- На каждый запрос выделяется 3 попытки, что отсеивает случайные ошибки со стороны источника
- Сервис полностью асинхронный, что увеличивает его пропускную способность

**Как сервис ведет себя при недоступности источника**:
На каждый запрос сервис выделяет 3 попытки, поэтому случайные ошибки со стороны источника не повлияют на ответ. При недоступности источника сервис вернет ответ с кодом 500 и информацией по ключу 'detail'

**Какие “неофициальные” моменты вы использовали (если использовали) и как снизили риск поломки**:
Сервис использует только официальные библиотеки и не прибегает к помощи неофициальных проектов.
> **Заметка**: была рассмотрена библиотека Yandex Music API by MarshalX, но в ходе анализа функционала их модуля пришел к выводу, что эффективнее использовать прямые запросы к источнику

Риск поломки был снижен посредством рассмотрения многих краевых случаев. Это гарантирует стабильную бесперебойную работу сервиса

**Что бы вы сделали дальше за +2-3 часа**
За следующие несколько часов я бы:
- ввел систему кэширования данных
- изучил API яндекс музыки доскональнее, потому что в текущей версии остается допущение о куке с айди сессии
- лучше разобрался в причинах ответа источника в роде капчи, чтобы ускорить время ответа при повторных попытках
